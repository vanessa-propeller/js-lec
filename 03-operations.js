// PEMDAS
let x = 20;
let y = 24;
let sum = x + y;
let difference = x - y;
let product = x * y;
let quotient = x / y;
let remainder = y % x;

console.log(sum, difference, product, quotient, remainder);

// NOT operator
let a = false;
console.log(!a); // prints 'NOT a' -> which is the opposite of false -> true
